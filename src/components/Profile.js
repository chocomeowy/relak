import { Typography } from "antd";
import React from "react";
const { Title } = Typography;
const Profile = () => {
  return (
    <div
      style={{
        textAlign: "center",
        padding: "10px",
      }}
    >
      <Title>profile.</Title>
      <div>profile</div>
    </div>
  );
};

export default Profile;
